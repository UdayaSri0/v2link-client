fn main() {
    // Placeholder crate for the future Aya eBPF object build.
    // The daemon currently reports the eBPF backend as unavailable rather than
    // pretending per-application counters are exact.
}
